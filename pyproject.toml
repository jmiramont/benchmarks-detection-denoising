[tool.poetry]
name = "benchmark-demo"
version = "0.1.0"
description = "A benchmark for testing tf methods."
authors = ["J. M. Miramont"]

[tool.poetry.dependencies]
python = "^3.8"
numpy = "^1.22.0"
matplotlib = "^3.5.1"
pandas = "^1.3.5"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
